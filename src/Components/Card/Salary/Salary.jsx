import PropTypes from 'prop-types';

const Salary = ({salary}) => {
    return (
        <div>
            <p>Salary: ${salary}</p>
        </div>
    );
};

Salary.propTypes = {
    
};

export default Salary;
import React from 'react';
import PropTypes from 'prop-types';

const Cart = props => {
    return (
        <div>
            <h3>This is Player Cart</h3>
        </div>
    );
};

Cart.propTypes = {
    
};

export default Cart;